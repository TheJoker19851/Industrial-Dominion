{
  "name": "@industrial-dominion/config",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src tests --max-warnings=0",
    "test": "vitest run tests",
    "typecheck": "tsc --noEmit",
    "format": "prettier --check \"src/**/*.ts\" \"tests/**/*.ts\" \"*.json\""
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
